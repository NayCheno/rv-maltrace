# Vivado Authorization Evidence

Phase 4.2 authorization and target-device risk record.

## Checks

```powershell
uv run rvmt vivado:check
uv run python tools/check_vivado_authorization.py
uv run python tools/check_vivado_authorization.py --self-test
```

## Status

| Risk Item | Status | Evidence | Residual |
| --- | --- | --- | --- |
| Vivado license supports the target device | PASS (artifact evidence) | Existing `bitstream:build` output includes `ariane_xilinx.bit`, `ariane_xilinx.mcs`, and `ariane_xilinx.dcp`; the routed timing report was generated by Vivado v2025.2 for `7k325t-ffg900` on 2026-05-08 | Future rebuilds still depend on the same Vivado/license environment; rerun `uv run rvmt vivado:check` and the authorization checker before rebuilding |
| Genesys 2 FPGA part can synthesize, implement, and generate bitstream | PASS | `ariane.timing.rpt` has `Design State : Routed`, Slack (MET) 0.177 ns; route status is 130,576/130,576 routable nets fully routed with 0 routing errors | Baseline `check_timing` still has known open constraint WARN rows recorded in `docs/03-platform-architecture/genesys2/board_bringup.md` |
| Board files are available | PASS | `uv run rvmt vivado:check` on 2026-05-08 reported `digilentinc.com:genesys2:part0:1.1`; `vendor/vivado-boards/new/board_files/genesys2/H` has `board.xml`, `mig.prj`, `part0_pins.xml`, and `preset.xml` | Keep the `vendor/vivado-boards` submodule present when moving worktrees |

This is not a physical board bring-up result. It only closes the pre-board
authorization, target part, and board-file risk called out in Phase 4.2.
