# 35T Surrogate Boot Provenance

Status: SURROGATE_BOOT_PROVENANCE_DEFERRED_RUN_SCOPED_LOG_MISSING

This package records whether the surrogate run has a run-scoped Linux boot log. A deferred status is an explicit evidence gap, not a surrogate behavior failure.
